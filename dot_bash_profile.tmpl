{{- if .bash.use_kiro_cli -}}
# Kiro CLI pre block. Keep at the top of this file.
[[ -f "${HOME}/Library/Application Support/kiro-cli/shell/bash_profile.pre.bash" ]] && builtin source "${HOME}/Library/Application Support/kiro-cli/shell/bash_profile.pre.bash"
{{- end }}

{{- if eq .chezmoi.os "linux" -}}
{{ template "env.bash-linux.tmpl" . }}
{{- else if eq .chezmoi.os "darwin" -}}
{{ template "env.bash-darwin.tmpl" . }}
{{- else if eq .chezmoi.os "windows" -}}
{{ template "env.bash-windows.tmpl" . }}
{{- end }}
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('{{ .language.python.conda_root }}/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "{{ .language.python.conda_root }}/etc/profile.d/conda.sh" ]; then
        . "{{ .language.python.conda_root }}/etc/profile.d/conda.sh"
    else
        export PATH="{{ .language.python.conda_root }}/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

# Load .bashrc if it exists
if [ -f ~/.bashrc ]; then
    source ~/.bashrc
fi

{{- if .bash.use_kiro_cli -}}
# Kiro CLI post block. Keep at the bottom of this file.
[[ -f "${HOME}/Library/Application Support/kiro-cli/shell/bash_profile.post.bash" ]] && builtin source "${HOME}/Library/Application Support/kiro-cli/shell/bash_profile.post.bash"
{{- end }}
