name: Delegator
description: Full coding agent with strong subagent delegation bias. Auto-loads cavecrew delegation rules and embeds them in the persona so the model prefers spawning subagents over doing everything inline.
