# Python Version & Standards

> Placeholder — not yet defined. Ask user before applying language version rules.

## Topics to cover when defined

- Minimum Python version (e.g., 3.12, 3.13)
- Feature policy (`match`/`case`, `type` keyword, `Self` type, etc.)
- Dependency Python version constraints in `pyproject.toml`
- Testing framework preference (pytest, unittest)
- Async/await usage policy
- Packaging strategy (wheel, standalone scripts, etc.)